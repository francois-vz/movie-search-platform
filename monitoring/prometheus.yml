global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "movie-search-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]

  # TODO: add mcp-server metrics target if/when exposed.
